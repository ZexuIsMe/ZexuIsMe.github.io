---
title: 竞赛三级-鸿蒙-02
date: 2025-10-24 09:47:51
tags: []
categories:
  - 鸿蒙 
---

## Step 1: 创建员工数组
① src > main > ets > pages > Index.ets
![img.png](img.png)

```ts
struct Index {
    //员工数组（列表）对象
    @State employees: Array<Employee>=[]
}
```

## Step2：获取员工信息列表
② src > main > utils > SqliteDB.ts

```ts
  //获取员工信息列表
async getEmployees():Promise<Employee[]>{
    let employees: Array<Employee>=[]
    let sql = "select * from employees"
    await this.rdbStore.querySql(sql).then(res => {
        res.goToFirstRow()
        while (res.goToNextRow()) {
            employees.push({
                id: res.getLong(res.getColumnIndex("id")),
                name: res.getString(res.getColumnIndex("name")),
                salary: res.getLong(res.getColumnIndex("salary")),
                dept: res.getString(res.getColumnIndex("dept"))
            } as Employee)
        }
    }).catch(err => {
        console.log("app", `查询出错：${err.code}  ${err.message}`);
    })
    return employees
}
```

## Step 3: 页面代码

```ts

  build() {
    Column() {
      Row(){
        //用列表容器显示员工列表
        List({ space:20 }) {
          ForEach(this.employees, (item:Employee, index) => {
            ListItem() {
              Row(){
                Text(index+1+"").fontSize(18).width("10%")
                Text(item.name).fontSize(18).width("25%")
                Text("￥"+item.salary+"").fontSize(18).fontColor(Color.Gray).width("20%")
                Text(item.dept).fontSize(18).width("30%")
              }
            }
          })
        }
        .width('100%')
        .height('100%')
        .padding({left:10,right:10,top:15})

      }.height('60%')
    }
    .height('100%')
  }
```

## Step 4: 获取数据用于页面显示

```ts
  //员工数组（列表）对象
  @State employees: Array<Employee>=[]

  // 执行初始化员工列表数据
  async aboutToAppear() {
    rdb.getEmployees().then(data => {
      this.employees = data
    })
```
