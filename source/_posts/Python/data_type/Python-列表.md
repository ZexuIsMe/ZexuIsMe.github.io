---
title: Python-列表
date: 2025-09-07 21:28:04
tags: [Python, list, 列表, 基本数据类型]
categories:
  - Python
  - 数据类型
---

- 可变数据类型，属于**引用数据类型**，
    - 引用数据类型的特点就是，一般情况下，即便元素相同，存储地址也是不同的
    - 链式赋值型的列表除外
- 修改、追加、删除都是针对列表内部，不会影响到存储地址，存储地址不会变
- 支持切片，若切片超出索引范围，返回空列表（[]）

```python
list = [1, 2, 3, 4, 5, 6]

list[2:5] = [13, 14, 15]
print(list) # [1, 2, 13, 14, 15, 6]

list[2:5] = []
print(list) # [1, 2, 6]
```

<mark>`list[2:5]`：可看做 [2, 5)，也就是下标 2,3,4</mark>

## 修改元素

    list = [1, 2, 3, 4, 5, 6]
    list[0] = 9
    print(list) # [9, 2, 3, 4, 5, 6]

若列表是空列表，空列表没有填充物，也就是没有下标
或者目标下标根本不存在，这样的无法通过下标进行赋值或修改

## 添加元素

> 插入元素

`list.insert(1, 'abc')` 向列表的索引位置的<mark>右侧</mark>插入一个元素

`list.insert(-2, 'abc')` 向列表的索引位置的<mark>左侧</mark>插入一个元素

> 追加元素

`list.append("efg")` 向列表的末尾（正向的下标的末尾）添加追加一个元素

## 删除元素

`list.pop()` 删除列表的最后一个元素，是影响列表本身的删除，会返回被移除的元素

`list.pop(index)` 通过提供指定的下标，移除指定位置处的元素

`list.remove(3)` 从列表中移除第一个是 3 的元素，且该方法没有返回值

`list.clear()` 清空列表

## 排序

`list.sort()` 升序排序，无返回值

`list.sort(reverse=True)` 降序排序，无返回值

`lista.reverse()` 反转列表，无返回值

    lista = [1,2,3,4,5]
    lista.reverse()
    print(lista) # [5, 4, 3, 2, 1]

## 其他方法

`list.count(5)` 统计5这个元素出现的次数

    x = [1, 2, 3, 4, 5, 6, 5, 4, 3, 2, 1]
    y = x.count(5)
    print(y)

`list.index(元素[, 起始下标][, 结束下标])`获取目标元素下标，返回目标元素下标

`list.copy()` 拷贝列表，属于浅拷贝，返回的是一个新列表，该列表是一个新的存储地址

    # x.cpoy()
    x = [1, 2, 3, 4, 5, 6, 5, 4, 3, 2, 1]
    y = x.copy()
    y[0] = 1
    print("x", x) # x 111
    print("y", y) # y 112

注意：如果是以赋值形式的完成的类似于复制的操作，它和源对象是同一个存储地址

    x = [1, 2, 3, 4, 5, 6, 5, 4, 3, 2, 1]
    z = y

    print("x: "id(x))
    print("z: "id(z))
