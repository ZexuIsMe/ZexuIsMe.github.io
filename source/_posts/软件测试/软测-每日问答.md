---
title: 软测-每日问答
date: 2025-08-21 08:35:05
tags: [软测, 每日问答]
categories: 
  - 软件测试
---

>  20250820 周三

## 简单描述一下 SQL语句的执行顺序
1. FROM/JOIN
2. WHERE
3. GROUP BY
4. HAVING
5. SELECT
6. ORDER BY
7. LIMIT
为什么说要关注SQL的执行顺序呢？因为【别名】的存在，一般情况下，SELECT 中做的【字段别名】，如`SELECT column AS temp_column`
若在 SELECT 前面的阶段中使用【别名】则大概率SQL执行时是会报异常的；
但也有特殊的，比如 SELECT中聚合函数的别名就突破普遍执行顺序在 HAVING 中使用

## 怎样的流程算是基本流，什么算是备选流，什么又算是异常流？
A：按照一般流程做完的一条龙服务就是基本流，
比如登录，
- 第一步：输入用户名，
- 第二步：输入密码
- 第三步：点击登录

登录调用登录接口传递数据，后端检测数据，数据库中存在该用户且密码与该用户的密码一一对应，返回前端200code
前端接收到接口响应的200code表示该用户可以登录，于是离开用户登录页面，跳转到首页位置
这个过程中没有任何额外的操作，也没有出现异常情况，即为基本流
什么又算是备选流呢？在基本流的基础上进行的拓展，在经历一番波折但最终的结果导向是符合预期结果的流程算是备选流
什么又算是异常流呢？在基本流或备选流的基础上进行的拓展或该流程出现意外情况，比如，用户名输入错误，其他均已填写，点击登录，系统提示：用户名或密码错误，
只要不在基本流和备选流的预期范围内的异常行为皆可算作异常流

## 边界值的上离点识别： [10, 59) 上点，离点，内点
上点：10,59
离点：9，58
内点：区间内的任意一点，不含开区间
那么如何确认离点的存在的呢？简单的记忆就是括号往内推一个数就是离点，比如59是开区间，往内推一个数，58，58就是离点
离点也可以是58.999999

## SQL语句中聚合函数COUNT（）在使用的时候需要注意些什么？
- 若要显示字段，需要配合 GROUP BY 一起使用
- COUNT() 会过滤掉 NULL，不将其纳入统计范围内
- 若统计的字段参数为数字0或者字符长度为零的空字符`''`，他们是被纳入统计范围内的

> 20250822 周五

## ISO25010质量模型的8个特性
A：可靠、易用、兼容、安全、可迁移、可维护、功能、效率

## 简述一下黑白灰盒的区别
黑：完全不考虑系统内部结构和代码实现，仅通过输入输出验证功能是否符合需求，**不依赖代码，适合测试人员（非开发）执行**；更贴近用户实际使用场景。
白：完全知晓系统内部代码、逻辑结构和实现细节，基于代码逻辑设计测试用例。**能发现代码级缺陷（如逻辑错误、死循环）**；测试更彻底（覆盖代码路径）。
灰：部分了解系统内部结构（如接口设计、数据库结构），但不深入代码细节，结合黑盒和白盒的思路。**兼顾功能验证和内部逻辑，测试效率更高**；无需深入代码，成本低于白盒。

## 描述一下INNER JION、LEFT JOIN、RIGHT JOIN
- INNER JOIN：全匹配，只匹配符合桥梁的数据；
- LEFT JOIN：左连接：以左表为主，左表全额输出，若右表不存在匹配记录则显示NULL作为填充参数
- RIGHT JOIN：右连接：以右表为主，右表全额输出，若左表不存在匹配记录则用NULL作为填充参数

## 简述一下GET、POST的区别
A：Get请求一般用于数据的获取，为什么这么说呢？因为GET请求的参数在链接上且是一般情况下是明文显示，这是一种高危的安全行为，另外还有大小限制，
POST请求一般用于数据的交互，比如增删改查上传这类操作，为什么这么说呢？因为POST请求的参数被封装在请求体中，相对安全，且没有大小限制，

> 20250825 周一

## SQL中的 `=` 和 `IN`，分别在什么时候使用 ？

## Cookie 和 Session 的区别？
Cookie: 安全程度低，信息存储在浏览器中，敏感信息需要加密处理；关掉浏览器任然存在；有大小限制
Session：安全程度高，信息存储在服务器中；关掉浏览器就会消失；理论上无大小限制

## 用例写作的要素
 - 用例编号
 - 用例标题
 - 重要程度
 - 预置条件
 - 输入
 - 操作步骤
 - 期望结果

## TCP和UDP的区别？

 TCP：传输慢，但稳定，适合对数据完整度有一定要求的场景，比如文件的上传
 UDP：传输快，但不稳定，适合对传输要求高但对数据精度不高的场景，比如常见的直播
tcp是基于连接的可靠的协议，而udp发送之前不需要建立连接，但是udp的传输效率高

> 20250827 周三

## 三次握手

## 四次挥手

## （linux）目前已学习的操作中，哪些操作可以命令行创建文件

1. touch file.txt
2. vi file.txt
3. cat > file.txt << EOF ... EOF 
4. cp file.txt file2.txt （暂且不算）
5. echo 123 > file.txt

## **Q:（linux）vim：如何进入编辑；如何保存并退出编辑；如何不保存并退出编辑，进入插入模式后如何退出插入模式的**

- i
- :wq
- q!
- ESC

> 20250829 周五

## （Linux） cat > file.txt << EOF ... EOF 和 cat >> file.txt << EOF ... EOF 分别是什么意思？
前者：覆盖写入
后者：追加写入

## （Linux）cd /、cd ~、cd .、cd .. 分别是什么意思？

cd /  回到根目录
cd ~  回到当前用户目录
cd .  当前目录
cd .. 返回上一级


## （Linux）rm -rf ./dir 和 rm -rf ./dir/* 的区别？
前者：删除该目录和该目录中的所有内容
后者：删除该目录中的所有内容

## （Linux）管道的运用：若要查看file.txt文件前10行的内容且显示行号，那么该命令如何编写呢？

    head file.txt | cat -n

> 20250902 周二

## 软件测试过程一般划分为几个阶段？每个阶段的测试重点是什么？

软件测试过程一般划分为单元测试、集成测试、系统测试、验收测试

单元测试的测试重点，关注单元内部的数据结构、逻辑控制、异常处理等等
集成测试的测试重点，关注模块之间的接口
系统测试的测试重点，关注整个系统相对于需求的符合程度

## 回归测试的策略有哪些？

完全回归策略

选择性回归策略，覆盖修改法、周边影响法、指标达成法

## MySQL中的 varchar 和 char 有什么区别

char：固定长度，若内容不够长度，那么会由空格代替
varchar: 不固定长度

## 什么是冒烟测试，冒烟测试通常在哪个阶段做？

以确保证主流程或关键功能的正常执行作为目的的测试为冒烟测试，可以是任意一个测试阶段的前置准备测试

> 20250903 周二

## OSI 七层模型

应用层
表示层
会话层
传输层
网络层
数据链路程
物理层

## C/S架构、B/S架构区别？

CS：需要用户配合安装、升级、卸载；可定制化；本地安装安全度相对较高；兼容性相对BS成本高
BS：无需安装、无痛升级且无需卸载，标准化生产，不可定制化；浏览器运行，安全程度相对较低，对敏感数据的传递需要加密处理；只需关注浏览器的兼容即可

## （Linux）如何杀掉进程？

kill -9 进程号

## 提交的缺陷开发不认可怎么办？

~向上级汇报缺陷情况，看上级怎么安排和行动~

与开发沟通找到不认可的点
》 先核对是否符合需求
》 考虑该缺陷是否涉及到隐性需求
》 向组长汇报该缺陷存在争议的原因

> 20250905 周五

## （Python）range(5) ，list\[2:]，tuple(-2:) 取值范围是？

range(5): 0, 1, 2, 3, 4
list\[2:]：从索引为 2 的元素开始，取到列表末尾的所有元素
tuple(-2:)：从索引为 -2 的元素开始，取到列表末尾的所有元素

**\[:::] 切片取值一般情况下都是从左往右的，若步长为负，则从右往左**

## （Python）2==1==1；1<10 or 1<5<=10；他们的返回值是？描述一下你的解题过程。

2==1==1 》 前真则后，否这为前 》 False
1<10 or 1<5<=10 》 前真则前，否则为后 》 True

## （Python）列表如何添加元素、如何移除指定元素？

添加元素：
insert(index, value)，通过插入的方式添加元素
append(value)，列表末尾添加一个元素

移除指定元素
pop(index)
remove(value)

## （Python） x = []; x[0] = 9 为什么无法通过运行？

因为是空列表，不存在索引（index）0，所以无法正常运行，若存在一个元素：x = \[1]，则语句通过

> 20250909 周二

## （Python）is 和 == 的区别？a=list(); b=list(); 问a==b，a is b的返回值

is 判断两侧是否来自同一个存储地址
== 判断两侧内容是否一致

    a = list()
    b = list()
    print(a == b) # True
    print(a is b) # False

## （Python）list 和 set 的区别？

**共同点**： 是可变的，可以添加、删除元素

**不同点**：
list 是被中括号包裹的对象，他是有序的，支持切片，允许重复的元素
set 是被花括号包裹的对象，它是无序的，因为是无序的所以不支持切片，不允许有重复的元素， 会自动去重，它添加的元素位置是随机的，连删除也是随机的，虽然也支持指定删除就是了；集合与集合之间支持运算，交集、并集、差集等等

## （Python）浅拷贝、深拷贝的区别？

浅拷贝：拷贝层级只有一层，若元素是只读对象，则修改不会影响源对象；若元素是可变的，如列表，则修改会影响源对象
深拷贝：拷贝层级是所有层级，与源对象完全脱离引用，无论怎么就该都不会影响源对象

```python
import copy

# 定义一个 变量x，赋予list对象
x = list([1, 2, 3, [4, 5]])
# 定义 变量y 并将 x 赋值给 变量y
y = x.copy()

print("x == y：", x == y) # 输出：True
print("x is y：", x is y) # 输出：False
print("-"*40)

y[0] = 100
y[-1].append(6)
y.append(1)
print(f"x：{x}") # [1, 2, 3, [4, 5]]
print(f"y：{y}") # [100, 2, 3, [4, 5, 6], 1]
print("-"*40)

# 深拷贝：copy.deepcopy(target)
z = copy.deepcopy(x)
z[0] = 200
z[-1].append(10)
print(f"x：{x}") # [1, 2, 3, [4, 5, 6]]
print(f"z：{z}") # [200, 2, 3, [4, 5, 6, 10], 1]
```

通过这段代码进一步验证了前面对深浅拷贝描述的正确性

## （Python）break 和 continue 的区别？

break：终止循环，让循环提前结束
continue：跳出当前循环，进入下一个循环

> 20250909 周三

## （Python）索引访问和切片访问有区别吗？如果有区别是？

有，

索引访问时，索引返回的是单个元素，若目标不存在或索引超出范围，终端会有错误异常；

索引目标如果是列表这样的引用数据类型，那么通过索引可以修改元素，且该修改会影响源对象

切片访问时，切片返回的是一个新的对象，可以获得多个元素且被该元素的符号包裹，即便是空或者只有一个都是如此，若目标不存在或超出范围会返回空对象；

## （Python）def func(**a, a, b="123", c)，函数参数有误，正确的应该是？

    def func(c, b="123", **a)

## （Python）描述一下递归，适用于哪些场景？

自己调用自己的行为称为递归，有调用上限，一般情况下超出1000，终端报错：超出上限

比如快排，将一个列表取一点，大于的放右侧，小于的放左侧，然后不停的循环该操作，直到分解到最小单元

## （Python）以下代码运行结果是？请描述一下它的运行过程

    x = 5
    y = 0 if x > 6 else print(666)
    print(y)  

输出：
    
    666
    None

> 20250912 周五

## python中模块和包的所有根路径，保存在哪个？

    sys.path

`sys.path` 是一个列表，包含了 Python 解释器在导入模块时会依次搜索的目录路径。
当使用 `import` 语句导入模块时，Python 会遍历 sys.path 中的路径，寻找对应的模块文件。

该列表中的路径通常包括：
- 当前执行脚本所在的目录
- Python标准库的安装路径
- 第三方库的安装路径（如 `site-packages` 目录）
- 通过环境变量 `PYTHONPATH` 配置的路径

## python使用什么符号实现语句的嵌套关系？

使用冒号和缩进实现语句的嵌套关系

冒号：用于标记一个代码块的开始，通常放在条件判断、循环、函数以及类定义等语句的末尾
缩进：用于表示代码块的范围，缩进的部分属于该代码块，形成嵌套关系，通常情况下以4个空格作为标准缩进

```python
for i in range(3):  # 冒号标记循环代码块的开始
    print(f"外层循环：{i}")  # 缩进表示属于外层循环
    if i > 0:  # 冒号标记条件代码块的开始
        print("  内层条件：i大于0")  # 更深的缩进表示嵌套在条件中
```

## （Python）类中的普通方法和类方法，各自的第一个参数名是什么、表示什么含义？

类中普通方法第一个参数：self，表示实力对象本身，可以通过它访问或修改实例的属性和其他实例方法
类中类方法的第一个参数：cls，表示类本身，可以访问类和修改类的属性和其他类方法

## （Python）面向对象的三大特性？

封装、多态、继承

> 20250915 周一

## （Python）lambda函数是什么，运用于什么场合？

lambda函数是一种没有名字的函数，也就是匿名函数，

它是一种表达式，可用来简化代码，执行结果就是要返回的值，无需使用 return 语句。

    a = lambda x: x+1
    a(1) # 返回结果：2

【适用场景】
1. 简单的临时函数
2. 高阶函数的参数，如`map`,`filter`, `sorted`等
3. 在闭包或回调中使用


    def func_1(n):
        return = lambda x, y: x+y

    func_1(1)(2) # 返回结果：3

## （Python）python中内置的数据类型有几种？

1. 字符（str）
2. 数字（int）
3. 浮点（float）
4. 列表（list）
5. 元组（tuple）
6. 集合（set）
7. 字典（dict）
8. 布尔（bool）
9. 范围（range）

## （Python）对缺省参数的理解 ？

缺省值，也就是常说的默认值，一般出现在函数的形参位置出现，
1. 表示若不传该参数，则使用默认值
2. 一般来说，缺省值的设置，表示该值的使用频率很高
3. 缺省参数必需在常规参数后面

## （Python）python中的可变类型和不可变类型？

【可变类型】 创建后可以直接修改其内部元素，不会生成新对象
如：list, dict
【不可变类型】 创建后无法修改其内部元素，修改后会生成新对象
如：int，float，bool, str, tuple（元组是一个很特殊不可变类型）

> 20250916 周二

## （Selenium）常见元素定位的方法有哪些？如何定位一组元素，为什么？

ID、NAME、TAG_NAME、CLASS_NAME、LINK_TEXT、PARTIAL_LINK_TEXT、CSS_SELECTOR、XPATH

类名、标签名、链接标签、XPATH、模糊匹配，配合 find_elements(By.定位方法, value) 都可定位一组元素。

因为他们的筛选目标基本都不是唯一的。

## （MySQL）常见的聚合函数有哪些？使用的时候需要注意什么？

SUM、AVG、COUNT、MAX、MIN
使用时需注意：
1. 若需要按分组筛选需要配合 HAVING 使用而不是 WHERE
2. 聚合函数的使用一般情况下都会和 GROUP BY 配套使用

## （MySQL）insert,update,delete语法？

【INSTR 语法】 `INSTR INTO 表名(字段1, 字段2...) VALUES(值1, 值2)`
【UPDATE 语法】 `UPDATE 表名 SET 字段1=值1 WHERE 条件`
【DELETE 语法】 `DELETE FROM 表名 WHERE 条件`

## 描述一下软件测试的定义以及测试方法有哪些？

【软件测试定义】采用人工或自动的方式，在运行软件或直接观察软件的过程中，判断软件是否满足用户需求，弄清软件与需求之间的差距
【测试方法】不是用例设计方法，是测试方法：
黑白灰、 动态静态、人工自动


> 20250918 周四

## 描述一下软件的生命周期？

计划阶段、规划阶段、需求设计阶段、编码、测试、运维阶段

## （MySQL）数据库中常见约束有哪些？

主键约束（primary key）、外键约束（foreign key）、唯一约束（unique）、非空约束（not null）、检查约束（check）

## （Python）阅读下面这段代码，其输出结果是：

```python
def func_1(item:str, x:list=[]):
   x.append(item)
   return x
print(func_1('1')) # ['1']
print(func_1('2')) # ['1', '2']
print(func_1('3')) # ['1', '2', '3']
```

这是 Python 中默认参数的“可变默认值陷阱”本质是因为默认值在函数定义时仅初始化一次，对于列表、字典等可变对象需特别注意。

函数的缺省值在函数定义时创建的，不会每次调用时重新创建。若不指定传入的实参 x，则调用默认参数 x 且指向同一个列表对象

每次调用 func_1 且未传入 x 时，都会复用这个默认列表，append 操作会累积到同一个列表中。

## （Linux）ipconfig、ifconfig、ping 三者的作用是，怎么使用它？

| ---      | ---                            |
|----------|--------------------------------|
| ipconfig | windows 环境下用于获取本机IP地址相关信息      |
| ifconfig | linux 环境下用于获取本机IP地址相关信息        |
| ping     | 用于检测本机与目标（IP 地址）之间的网络连通性       |
|          | Eg: ping https://www.baidu.com |